server:
  port: 8001

spring:
  application:
    name: security-server
  datasource:
    url: jdbc:mysql://localhost:3306/cloud-vote?useSSL=FALSE&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
logging:
  level:
    com.example.bdatabaserole.mapper: debug  # 打印sql语句
